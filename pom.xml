<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.freakmaxi</groupId>
    <artifactId>locking-center-client</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>Locking-Center Java Client</name>
    <description>Java client for Locking-Center, a TCP mutex server</description>
    <url>https://github.com/freakmaxi/locking-center-client-java</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
        </license>
    </licenses>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- server used by the integration tests, override with -Dlockingcenter.address=host:port -->
        <lockingcenter.address>127.0.0.1:22119</lockingcenter.address>
        <lockingcenter.skipIntegration>0</lockingcenter.skipIntegration>
    </properties>

    <!-- no dependencies, the library and its tests only need the JDK -->

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
            </plugin>
            <!-- the tests are a plain main-based runner, so they run identically with and without Maven -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.2</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>run-tests</id>
                        <phase>test</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <skip>${skipTests}</skip>
                            <executable>java</executable>
                            <classpathScope>test</classpathScope>
                            <arguments>
                                <argument>-classpath</argument>
                                <classpath/>
                                <argument>com.freakmaxi.lockingcenter.TestRunner</argument>
                            </arguments>
                            <environmentVariables>
                                <LOCKING_CENTER_ADDRESS>${lockingcenter.address}</LOCKING_CENTER_ADDRESS>
                                <LOCKING_CENTER_SKIP_INTEGRATION>${lockingcenter.skipIntegration}</LOCKING_CENTER_SKIP_INTEGRATION>
                            </environmentVariables>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
