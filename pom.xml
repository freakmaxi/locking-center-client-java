<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- io.github.<user> is verifiable via GitHub; use com.freakmaxi only if you own freakmaxi.com -->
    <groupId>io.github.freakmaxi</groupId>
    <artifactId>locking-center-client</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>Locking-Center Java Client</name>
    <description>Java client for Locking-Center, a TCP mutex server</description>
    <url>https://github.com/freakmaxi/locking-center-client-java</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>freakmaxi</id>
            <name>freakmaxi</name>
            <url>https://github.com/freakmaxi</url>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/freakmaxi/locking-center-client-java.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/freakmaxi/locking-center-client-java.git</developerConnection>
        <url>https://github.com/freakmaxi/locking-center-client-java</url>
    </scm>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- server used by the integration tests, override with -Dlockingcenter.address=host:port -->
        <lockingcenter.address>127.0.0.1:22119</lockingcenter.address>
        <lockingcenter.skipIntegration>0</lockingcenter.skipIntegration>
    </properties>

    <!-- no dependencies, the library and its tests only need the JDK -->

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
            </plugin>
            <!-- the tests are a plain main-based runner, so they run identically with and without Maven -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.2</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>run-tests</id>
                        <phase>test</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <skip>${skipTests}</skip>
                            <executable>java</executable>
                            <classpathScope>test</classpathScope>
                            <arguments>
                                <argument>-classpath</argument>
                                <classpath/>
                                <argument>com.freakmaxi.lockingcenter.TestRunner</argument>
                            </arguments>
                            <environmentVariables>
                                <LOCKING_CENTER_ADDRESS>${lockingcenter.address}</LOCKING_CENTER_ADDRESS>
                                <LOCKING_CENTER_SKIP_INTEGRATION>${lockingcenter.skipIntegration}</LOCKING_CENTER_SKIP_INTEGRATION>
                            </environmentVariables>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Publishes to Maven Central via the Central Portal. Needs a
                 <server> named "central" in ~/.m2/settings.xml holding your
                 Central Portal user token. autoPublish=false uploads a
                 deployment you then release from the portal UI; set it to true
                 once you trust the pipeline. Check for a newer plugin version
                 if this one fails to resolve. -->
            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
                <version>0.11.0</version>
                <extensions>true</extensions>
                <configuration>
                    <publishingServerId>central</publishingServerId>
                    <autoPublish>false</autoPublish>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <!-- Activate with `mvn -Prelease clean deploy` to build the sources and
         javadoc jars, GPG-sign every artifact (all three are Central
         requirements) and upload. A normal `mvn package` skips all of this,
         so it needs no GPG key. -->
    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <version>3.3.1</version>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <goals>
                                    <goal>jar-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <version>3.6.3</version>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <!-- do not fail the release on javadoc lint nits -->
                            <doclint>none</doclint>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.2.4</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                                <configuration>
                                    <!-- needed for non-interactive signing on
                                         modern GnuPG; drop if your key prompts
                                         interactively -->
                                    <gpgArguments>
                                        <arg>--pinentry-mode</arg>
                                        <arg>loopback</arg>
                                    </gpgArguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
